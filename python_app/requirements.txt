# python_app dependencies
# Install via: mise run install (preferred) or pip install -r python_app/requirements.txt

# Core TTS engines
edge-tts>=7.2.8
piper-tts>=1.5.0

# PyTorch + audio (Piper)
# torch and torchaudio are optional GPU-acceleration dependencies — install
# manually when a GPU is available: pip install torch torchaudio
# They are NOT required for Edge-TTS + Piper (the default workflow).
numpy>=2.5.1,<3.0.0
soundfile>=0.14.0

# Ebook processing
beautifulsoup4>=4.15.0
ebooklib>=0.20
pypdf>=6.14.2
lxml>=6.1.1

# Language detection
langdetect>=1.0.9,<2.0.0

# Audio
static-ffmpeg
mutagen>=1.48.1

# Web server
fastapi!=0.136.3,>=0.139.2
uvicorn[standard]>=0.51.0
python-multipart>=0.0.32

# Data
pydantic>=2.13.4,<3.0.0
pandas>=3.0.5,<4.0.0
fsspec<=2026.6.0,>=2026.6.0
packaging>=26.2,<27.0

# HTTP / async
aiohttp>=3.14.3,<4.0.0
aiofiles>=25.1.0
httpx>=0.28.1
certifi>=2026.7.22
requests>=2.34.2,<3.0.0

# CLI
tqdm
argcomplete>=3.7.0

# HF
huggingface_hub>=1.24.0
transformers>=5.14.1

# Progress / monitoring
psutil>=7.2.2

# Audio verification
faster-whisper>=1.2.1

# Security pins (CVE fixes)
pillow>=12.3.0
werkzeug>=3.1.8

# Testing
pytest
pytest-asyncio
