name: Auto Release

# Automatically creates a new version tag after every successful CI run on master.
# The workflow must not push follow-up commits to master because the branch is
# protected by required checks. It only tags the already-validated HEAD commit.

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

on:
  workflow_run:
    workflows: ["CI"]
    types: [completed]

permissions:
  actions: write
  contents: write

jobs:
  auto-release:
    name: Auto version tag
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.event == 'push' &&
      github.event.workflow_run.head_branch == 'master' &&
      github.event.workflow_run.head_repository.full_name == github.repository
    steps:
      - name: Checkout
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
          ref: master
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Validate shared app version
        id: version
        run: |
          set -euo pipefail
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          import re
          from pathlib import Path

          python_version = re.search(
              r'__version__ = "(.+)"',
              Path("python_app/version.py").read_text(encoding="utf-8"),
          ).group(1)
          web_version = json.loads(
              Path("web/package.json").read_text(encoding="utf-8")
          )["version"]

          versions = {
              "python_app/version.py": python_version,
              "web/package.json": web_version,
          }
          unique_versions = set(versions.values())
          if len(unique_versions) != 1:
              details = ", ".join(f"{path}={version}" for path, version in versions.items())
              raise SystemExit(f"Version mismatch across manifests: {details}")

          changelog = Path("CHANGELOG.md").read_text(encoding="utf-8")
          if f"## [{python_version}]" not in changelog:
              raise SystemExit(
                  f"CHANGELOG.md is missing an entry for version {python_version}"
              )

          print(f"base_version={python_version}")
          PY

      - name: Determine next version
        id: bump
        run: |
          set -euo pipefail

          LAST_TAG=$(git describe --tags --abbrev=0 --match 'v[0-9]*.[0-9]*.[0-9]*' 2>/dev/null || echo "")
          BASE_VERSION="${{ steps.version.outputs.base_version }}"

          RANGE="${LAST_TAG:+${LAST_TAG}..}HEAD"

          NEW=$(python3 - <<'PY' "$BASE_VERSION" "$LAST_TAG"
          import sys

          def parse(value: str):
              return tuple(int(part) for part in value.split("."))

          base = parse(sys.argv[1])
          tag = sys.argv[2]

          if not tag:
              print(sys.argv[1])
              raise SystemExit

          last = parse(tag[1:])

          if base > last:
              print(sys.argv[1])
          else:
              print("")
          PY
          )

          if [[ -z "$NEW" ]]; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "base_version=$BASE_VERSION" >> "$GITHUB_OUTPUT"
            echo "last_tag=${LAST_TAG}" >> "$GITHUB_OUTPUT"
            echo "range=${RANGE}" >> "$GITHUB_OUTPUT"
            echo "Committed version ${BASE_VERSION} is not newer than ${LAST_TAG}; skipping release tag"
            exit 0
          fi

          if git rev-parse "v${NEW}" >/dev/null 2>&1; then
            echo "skip=true" >> "$GITHUB_OUTPUT"
            echo "base_version=$BASE_VERSION" >> "$GITHUB_OUTPUT"
            echo "last_tag=${LAST_TAG}" >> "$GITHUB_OUTPUT"
            echo "range=${RANGE}" >> "$GITHUB_OUTPUT"
            echo "Tag v${NEW} already exists; skipping"
            exit 0
          fi

          echo "skip=false" >> "$GITHUB_OUTPUT"
          echo "base_version=$BASE_VERSION" >> "$GITHUB_OUTPUT"
          echo "last_tag=${LAST_TAG}" >> "$GITHUB_OUTPUT"
          echo "range=${RANGE}" >> "$GITHUB_OUTPUT"
          echo "version=$NEW" >> "$GITHUB_OUTPUT"
          echo "tag=v$NEW" >> "$GITHUB_OUTPUT"
          echo "Version: ${BASE_VERSION} -> ${NEW} (last tag: ${LAST_TAG:-none})"

      - name: Create release tag
        if: steps.bump.outputs.skip != 'true'
        run: |
          set -euo pipefail
          if git rev-parse "${{ steps.bump.outputs.tag }}" >/dev/null 2>&1; then
            echo "Tag ${{ steps.bump.outputs.tag }} already exists — skipping"
            exit 0
          fi
          git tag "${{ steps.bump.outputs.tag }}"
          git push origin "${{ steps.bump.outputs.tag }}"

      - name: Dispatch Release Desktop (new version)
        if: steps.bump.outputs.skip != 'true'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          gh workflow run release-desktop.yml \
            --ref master \
            -f tag="${{ steps.bump.outputs.tag }}"

      - name: Dispatch Release Desktop (rolling rebuild)
        if: steps.bump.outputs.skip == 'true' && steps.bump.outputs.last_tag != ''
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          echo "Rolling rebuild: updating artifacts on ${{ steps.bump.outputs.last_tag }}"
          gh workflow run release-desktop.yml \
            --ref master \
            -f tag="${{ steps.bump.outputs.last_tag }}" \
            -f checkout_ref="master"
