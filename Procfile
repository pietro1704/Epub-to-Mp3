# Railway Procfile - Define how to run the application

# Web server (FastAPI)
web: cd python_app && uvicorn server:app --host 0.0.0.0 --port $PORT

# Telegram Bot (opcional - comentado por padrão, descomente se quiser rodar no Railway)
# bot: cd python_app && python telegram_bot.py
