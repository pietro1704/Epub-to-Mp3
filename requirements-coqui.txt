# Coqui XTTS engine — optional, install only if you need GPU-accelerated TTS
# Install: pip install -r requirements-coqui.txt
# Requires: CUDA GPU recommended; CPU works but is very slow

TTS>=0.22.0,<0.23.0
numba==0.66.0
llvmlite==0.48.0
