# Environment variables for PRODUCTION build
# Copy this to .env.production and fill with your Railway backend URL

# Backend API URL (REQUIRED for Cloudflare Pages)
VITE_API_BASE=https://seu-app-production.up.railway.app

# Poll interval in milliseconds (optional)
# VITE_POLL_INTERVAL_MS=3000
