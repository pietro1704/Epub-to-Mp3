# Hugging Face Space Requirements
# EPUB to MP3 Converter with Edge-TTS

# FastAPI Server
fastapi>=0.134.0
uvicorn[standard]>=0.41.0
python-multipart>=0.0.22

# TTS Engine (Edge-TTS - Microsoft online)
edge-tts>=7.2.0
 
# Best offline fallback (quality + multilingual, CPU-only)
TTS>=0.22.0,<0.23.0
numba==0.58.1
llvmlite==0.41.1
torch>=2.2.0
torchaudio>=2.2.0
soundfile>=0.12.1
pandas>=1.5.0,<4.0.0

# Offline fallback (fast, small)
piper-tts>=1.3.0

# Kokoro TTS (fast, lightweight 82M model)
kokoro>=0.9.4

# Spark-TTS dependencies (LLM-based, voice cloning)
huggingface_hub>=0.20.0
transformers>=4.36.0

# Ebook Processing
ebooklib>=0.18
beautifulsoup4>=4.12.0
pypdf>=6.8.0
lxml>=5.0.0

# Language Detection
langdetect>=1.0.9

# Audio Processing
static-ffmpeg
mutagen>=1.47.0

# Audio Verification (speech-to-text)
faster-whisper>=1.0.0

# Data/Validation
pydantic>=2.10.0
numpy>=1.24.0,<2.0.0

# HTTP/Async
aiohttp>=3.9.0
aiofiles>=24.0.0
httpx>=0.28.1

# Progress
tqdm

# System monitoring
psutil>=7.2.2

# Security pins (transitive dependency CVE fixes)
pillow>=12.1.1  # CVE-2026-25990
werkzeug>=3.1.6  # CVE-2026-27199

# Testing (dev only)
pytest
pytest-asyncio
