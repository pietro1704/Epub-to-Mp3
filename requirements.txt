# Hugging Face Space Requirements
# EPUB to MP3 Converter with Edge-TTS

# FastAPI Server
fastapi>=0.136.1
uvicorn[standard]>=0.46.0
python-multipart>=0.0.28

# TTS Engine (Edge-TTS - Microsoft online)
edge-tts>=7.2.8

# Piper audio deps
torch>=2.6.0  # PYSEC-2024-259, PYSEC-2025-41
torchaudio>=2.11.0
soundfile>=0.13.1
pandas>=3.0.3,<4.0.0

# Offline fallback (fast, small)
piper-tts>=1.4.2

# Spark-TTS dependencies (LLM-based, voice cloning)
huggingface_hub>=1.15.0
transformers>=5.8.1

# Ebook Processing
ebooklib>=0.20
beautifulsoup4>=4.14.3
pypdf>=6.11.0  # CVE-2026-33699, GHSA-3crg-w4f6-42mx
lxml>=6.1.0

# Language Detection
langdetect>=1.0.9

# Audio Processing
static-ffmpeg
mutagen>=1.47.0

# Audio Verification (speech-to-text)
faster-whisper>=1.2.1

# Data/Validation
pydantic>=2.13.4
numpy>=2.4.4,<3.0.0

# HTTP/Async
aiohttp>=3.13.5  # CVE-2026-34513–34520, CVE-2026-22815, CVE-2026-34514
aiofiles>=25.1.0
httpx>=0.28.1

# Progress
tqdm

# System monitoring
psutil>=7.2.2

# Security pins (transitive dependency CVE fixes)
pillow>=12.2.0      # CVE-2026-25990
werkzeug>=3.1.8     # CVE-2026-27199
flask>=3.1.3        # CVE-2026-27205 (via TTS)
nltk>=3.9.4         # CVE-2025-14009, CVE-2026-33230, GHSA-rf74-v2fm-23pw (via TTS)
pygments>=2.20.0    # CVE-2026-4539 (via rich/pytest)
requests>=2.34.2    # CVE-2026-25645 (via huggingface-hub, transformers)
filelock>=3.29.0    # CVE-2026-22701 (via huggingface-hub, torch)
jaraco-context>=6.1.2  # CVE-2026-23949 (via keyring)
setuptools>=82.0.1  # PYSEC-2022-43012, PYSEC-2025-49, CVE-2024-6345
virtualenv>=21.3.3 # CVE-2026-22702 (via pre-commit)

# Additional security pins surfaced by `mise run audit` (OSV)
python-dotenv>=1.2.2  # CVE-2026-28684
python-multipart>=0.0.26  # CVE-2026-40347 (already pinned via direct dep above; redundant safeguard)

# Testing (dev only)
pytest>=9.0.3  # CVE-2025-71176
pytest-asyncio
