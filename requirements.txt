# Hugging Face Space Requirements - EPUB to MP3 Converter
# Minimal dependencies for Edge-TTS only (no PyTorch/Coqui)

# Gradio UI
gradio>=4.0.0

# Core TTS Engine (Edge-TTS - Microsoft, online, no heavy dependencies)
edge-tts>=7.2.0
aiohttp>=3.13.0
certifi>=2025.10.0

# Ebook processing
beautifulsoup4>=4.12.0
ebooklib>=0.18
pypdf>=4.0.0

# Audio processing
numpy>=1.24.0,<2.0.0
langdetect>=1.0.9,<2.0.0

# Progress and CLI
tqdm

# Utils
requests>=2.32.0,<3.0.0
