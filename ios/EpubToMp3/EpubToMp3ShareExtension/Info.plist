<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>CFBundleDisplayName</key>
    <string>EpubToMp3</string>
    <key>NSExtension</key>
    <dict>
        <key>NSExtensionAttributes</key>
        <dict>
            <!-- UTI predicate: every attachment of every extension item
                 must conform to one of the EPUB/PDF UTIs. The previous
                 version only capped attachment COUNT (<= 10), so the
                 extension appeared in the Share Sheet for photos, plain
                 text, anything — ShareViewController.swift already
                 filters by UTI when actually importing, but the sheet
                 entry itself was unconditional (App Review 2.5/4.2). -->
            <key>NSExtensionActivationRule</key>
            <string>SUBQUERY (extensionItems, $extensionItem, SUBQUERY ($extensionItem.attachments, $attachment, ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "org.idpf.epub-container" || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.apple.ibooks.epub" || ANY $attachment.registeredTypeIdentifiers UTI-CONFORMS-TO "com.adobe.pdf").@count == $extensionItem.attachments.@count).@count == extensionItems.@count</string>
        </dict>
        <key>NSExtensionPrincipalClass</key>
        <string>EpubToMp3ShareExtension.ShareViewController</string>
        <key>NSExtensionPointIdentifier</key>
        <string>com.apple.share-services</string>
    </dict>
</dict>
</plist>
